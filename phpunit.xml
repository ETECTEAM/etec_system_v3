<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>

        <!-- Force an isolated schema so RefreshDatabase's migrate:fresh never
             touches real data. force="true" is required because docker-compose
             injects DB_DATABASE=etec_db as a real OS env var into the container,
             and there is no .env.testing to override it. -->
        <env name="DB_CONNECTION" value="mysql" force="true"/>
        <env name="DB_DATABASE" value="etec_db_test" force="true"/>

        <!-- Force this empty so ValidTurnstile stays a no-op during tests,
             the same way DB_DATABASE above overrides docker-compose's real
             OS env var - a real TURNSTILE_SECRET_KEY in .env would otherwise
             make every /instructor-register test fail on a missing captcha
             token the suite can't solve. -->
        <env name="TURNSTILE_SECRET_KEY" value="" force="true"/>
    </php>
</phpunit>
